# Contents

**Linux**
```
- SSH, bash
```

**Containerland**
```
- Containers in General
```

## Basics
```
-- Resource Isolation (cgroups, namespaces), Container Runtimes (runc, crio), Container Images
-- `docker build`: Dockerfiles, Layer Caching, copy-on-write, build engines
-- `docker run`:  Port Mappings, Volume Mounts, Environment Variables
-- `docker ps`
-- `docker exec`: "SSHing" into a container 
-- `docker push`: Pushing to a registry
-- `docker pull`: Pulling from a registry
-- `docker compose`: Configuring the run and build arguments in a yaml file, enabling a unit a service
```

## Intermediate
```
-- Inter-Container Networking
```

## Advanced
```
-- CI/CD
-- Multi-Stage Builds
-- Dev Containers
-- Remote Hosts
-- CUDA 
- MS SQL Containers
```


**Clusterland**
```
- Kubernetes in General
-- Control Plane, Kubernetes Agent, Pods
-- Kubernetes Resources: Pod, Service, Deployment
-- kubeconfig, kubectl, Web UI
-- Kubernetes Storage: Persistent Volumes, Persistent Volumes Claims
- MS SQL on Kubernetes
-- Helm Deployments
-- SQL Server Cluster System
```